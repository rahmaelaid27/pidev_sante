


{% block body %}
    <h1>Register</h1>

    {{ form_start(form) }}
    {{ form_row(form.nom) }}
    {{ form_row(form.email) }}
    {{ form_row(form.password) }}
    {{ form_row(form.roles) }}
    {{ form_row(form.submit) }}
    {{ form_end(form) }}
{% endblock %}
